{
  "name": "maxpain-d1-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute maxpain-db --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
